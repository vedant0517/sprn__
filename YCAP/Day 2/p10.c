// 173 Vedant
#include <stdio.h>
int main()
{
    //  int num;
    // printf("Enter Number\n");
    // scanf("%d",&num);

    // (num > 0) ? printf("Positive\n") : (num < 0) ? printf("Negative\n") : printf("Zero\n");
    // return 0;


    // int x=(20 || 40) && (10);
    // printf("x=%d",x);
    int a=3,b=2;
    a=a==b==0;
    printf("%d %d",a,b);

    

}